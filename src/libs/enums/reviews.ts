/**
 * Enum representing available review filter options.
 */
export const enum ReviewFilter {
  POSITIVE = 'POSITIVE',
  NEGATIVE = 'NEGATIVE',
  ALL = 'ALL',
}

/**
 * Enum representing available review sorting options.
 */
export const enum ReviewSort {
  NEWEST = 'NEWEST',
  OLDEST = 'OLDEST',
}
